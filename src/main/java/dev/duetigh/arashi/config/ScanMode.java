package dev.duetigh.arashi.config;

/** Whether {@link dev.duetigh.arashi.scanner.BlockScanner} highlights every match (MULTI) or a single tracked vein (TRACKING). */
public enum ScanMode {
	MULTI,
	TRACKING
}
